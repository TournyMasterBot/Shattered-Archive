The 'sdks-client' folder contains the client-side SDK package configuration and TypeScript settings for building a Node.js-compatible SDK. It defines the package structure, compilation settings, and build scripts needed to create the client library. This folder serves as the core configuration layer that enables consistent TypeScript compilation and package building for the SDK client.
