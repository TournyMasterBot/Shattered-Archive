The 'sdks-server' folder contains the server-side SDK package configuration and TypeScript settings for building and compiling the SDK server component. It defines the package metadata, build scripts, and TypeScript compilation options needed to create the server-side SDK functionality. This folder serves as the core configuration layer that enables proper compilation and packaging of the SDK server codebase.
