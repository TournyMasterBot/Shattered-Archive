The `src` folder contains the main source code files for the project, with `index.ts` serving as the entry point that exports key functions like the server greeting and ANSI stripping utility. The `strip-ansi.ts` file provides functionality to remove ANSI escape codes from strings, supporting the overall text processing capabilities of the application. Together these files form the core logic that handles server responses and text formatting within the project.
