{
  "name": "@shatteredarchive/utils-server",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --config ../../jest.config.cjs --runInBand --testPathPatterns utils/utils-server",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,jsx,json}\""
  }
}