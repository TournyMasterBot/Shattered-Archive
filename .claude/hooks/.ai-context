The hooks folder contains middleware functions that intercept and modify command execution flows to improve efficiency and prevent resource abuse. These hooks integrate into the project's workflow by automatically injecting plan context into AI prompts, guarding against expensive operations like long-output commands and full file reads, and updating queue files with workspace paths for method annotation. They act as protective layers that ensure commands execute safely and efficiently while maintaining continuity of ongoing work.
