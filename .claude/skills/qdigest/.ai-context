The qdigest folder contains functionality for delegating large local file and command processing to a free Qwen model rather than consuming Claude tokens. It serves as an alternative processing pipeline that reduces costs while maintaining similar capabilities. This folder integrates into the project by providing a token-efficient method for handling large-scale data processing tasks.
