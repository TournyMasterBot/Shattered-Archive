The 'gmcp-types' folder contains TypeScript data structure definitions that form the core data models for the game's GMCP (Generic MUD Communication Protocol) implementation. These files define the standardized formats for character stats, room information, affect statuses, login data, and market tick data that enable consistent communication between the client and server. The folder serves as the foundational type system that ensures all game data flows adhere to defined structures throughout the project.
