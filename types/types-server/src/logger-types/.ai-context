The 'logger-types' folder contains the foundational logging interface and enum definitions that standardize how application logs are structured and formatted throughout the project. It provides a configurable logging system that ensures consistent logging practices across different modules and components. This folder serves as a central configuration point for all logging-related type definitions and constants used throughout the application's logging infrastructure.
