The nginx folder contains all configuration files and scripts needed to set up and manage the NGINX reverse proxy server that routes traffic to different backend services based on subdomain headers. It includes configurations for static content delivery, API routing, SSL termination with self-signed certificates, and log management through logrotate and crontab automation. The folder serves as the central hub for defining how incoming requests are handled and forwarded to the appropriate microservices or applications within the project.
