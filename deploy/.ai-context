The deploy folder contains all production-ready Docker configurations and deployment scripts that orchestrate the complete application stack across multiple services. It includes docker-compose.yml for production deployment, individual service Dockerfiles for building the game and web clients/servers, and supporting documentation for deployment workflows. This folder serves as the definitive source for running the Shattered Archive project in production environments.
