The nightly-build folder contains automated deployment scripts that handle the regular deployment of the Shattered Archive application using Docker Compose. It serves as the automated pipeline for rolling out nightly builds to production environments. This folder is essential for maintaining consistent, automated deployment processes that ensure the application stays up-to-date with the latest changes.
