File Index

/add-hosts.ps1
Purpose: This PowerShell script automatically updates the system hosts file to map local development domains to localhost for a Shattered Archive project.

/add-hosts.sh
Purpose: This script modifies the system hosts file to map local domain names for a development environment.

/firewall-open-ports.ps1
Purpose: This PowerShell script automates creating firewall rules to open specific ports for a project in different environments.

/firewall-open-ports.sh
Purpose: Opens specified network ports for a project based on environment mode using available firewall tools.

/mkcert-init.sh
Purpose: Initializes local development SSL certificates for the shatteredarchive project.

/remove-hosts.ps1
Purpose: Removes ShatteredArchive entries from the system hosts file.

/remove-hosts.sh
Purpose: Removes ShatteredArchive entries from the system hosts file.

/setup-certs.ps1
Purpose: Generates self-signed SSL certificates for local development using mkcert.

/setup-certs.sh
Purpose: Generates local development SSL certificates for multiple project domains using mkcert.
