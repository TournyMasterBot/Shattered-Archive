The 'net' folder contains the network protocol implementation for the Kingdom Tactics engine, handling client-server communication through the KT protocol version pinning and message parsing logic. It serves as the foundational layer for all networked gameplay functionality, ensuring proper serialization and deserialization of game messages between clients and servers. The protocol.ts file defines the wire format while protocol.test.ts validates its correct implementation.
