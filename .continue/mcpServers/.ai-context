The 'mcpServers' folder contains configuration files that define server connections for MCP (Model Control Protocol) services used in the project. The shattered-archive-mcp-server.yaml file specifically configures the connection to the Shattered Archive MCP server, enabling the application to communicate with this particular service. This folder serves as the central location for all MCP server configurations, allowing the project to easily manage and switch between different server connections as needed.
