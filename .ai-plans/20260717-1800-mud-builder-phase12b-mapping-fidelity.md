# Plan: MUD Builder — Phase 12b (mapping fidelity: expanded compass rose, teleports, one-ways, blocked paths + UX/auth corrections)

Created: 2026-07-17T18:00:00Z · Workspace: /workspace/shattered-archive · Status: COMPLETE
Task: Correct the Phase 12 map's world model per the user's directions (10-direction rose, teleports, one-way/loop-back/asymmetric exits, object-blocked paths — DslMapper is the prior-art reference), plus the batched UX fixes (select contrast, grouped resets, world-limit flags, invalid-ref flagging) and the auth rework (auth/ folder + master-key rotation script).

## Goal
The engine, the .are model, the builder editors, and the Map tab all agree on a 10-direction compass rose (N/E/S/W/U/D + NE/NW/SE/SW); the map renders one-way, loop-back, asymmetric-return, door-blocked, and teleport exits distinctly with a legend; builders can author teleports (room-script warp) and door-blocked paths; resets display as color-grouped mob units; the world inspector flags limit-pressured items; selects are readable; unresolvable refs are flagged INVALID (resolved ones linkified); the master key lives under auth/ and is rotatable via generate-master-key.sh (child keys wiped). Done when all suites are green, the stock corpus boots byte-identically on the upgraded engine, and the live E2E passes.

## Constraints
- **Engine C changes ARE sanctioned this phase** (explicit user direction — the rose/teleports "represent what the mud should be allowed to build"). Keep them ADDITIVE and backward-compatible: doors 0-5 keep their exact numbers/semantics, every stock .are must load and behave identically, `make` stays warning-clean. The engine deploys ONCE, in step 7, via compose recreate (brief dev-time downtime accepted by the user); every other step is hot for the game.
- **Door numbering is decided here so all steps agree: 6=NE, 7=NW, 8=SE, 9=SW** (SMAUG lineage order — DSL itself is SMAUG-derived). Do not renumber 0-5.
- Builder tenets still hold: preview-first, writes gated by MUD_WRITE_ENABLED, bearer auth on mutations, audit lines never contain token values, never-crash server, all code testable in isolation.
- DslMapper is REFERENCE ONLY (types/semantics), its renderer is not reused.
- qwen (container) must NOT run pnpm install|build|test (host job); C compiles are host/engine-container jobs, never qwen's.
- Colorize reset groups ONLY where grouping exists (bare rows stay as-is); no two adjacent mob groups share a color.

## Context
- Engine is vanilla Merc 2.4 with exactly 6 dirs: /workspace/merc-mud/2.4/src/act_move.c:41 (`dir_name[]` north..down), comm.c:1297 (prompt "N E S W U D"), act_info.c:1265+ (autoexits), scan.c. db.c:951 rejects door>5 at boot (fatal); db.c:820 'D' reset door bound 0-5; db.c:838 'R' reset bound 0-6; db.c:965-977 `locks` 0-4 → EX_ISDOOR|PICKPROOF|NOPASS combos. Script engine is mob_prog.c/mob_prog.h (mob-attached triggers; hot-loaded via the Phase 2 mechanism, see area_reload.c) — rooms have no triggers today, and stock Merc has NO teleport room flag (merc.h ROOM_* list).
- Reset limit semantics (for the world-limit flags): db.c:1301 'M' skips when `pMobIndex->count >= arg2`; db.c:1380-1385 (O/P) and 1473-1480 (G/E) object limit = arg2, with `-1` = unlimited and `>50` = old-format sentinel; at limit only a 1-in-5 random lets one through. So an object/mob whose spawn references exceed its declared limits will stop appearing — computable statically from the parsed resets.
- Prior art: /workspace/dsl-mapper/game-mapping/src/types.ts — 10-dir `Direction`, `ExitDef.oneWay`, `Door` (simple/locked+keyId), `Interaction` verbs (dig/push/pull/shove) with `Effect`s (CreateExit/RevealExit/UnlockDoor/ToggleDoor/**Warp**); render/dir.ts — diagonal grid deltas NE(1,-1) SE(1,1) SW(-1,1) NW(-1,-1) and `reverseDir` (the asymmetry test: reverse exit missing = one-way; reverse exists but targets elsewhere = non-returning).
- Our model: /workspace/shattered-archive/services/merc-area/src/types.ts RoomExit `{door 0-5, description, keyword, locks, key, toVnum}`; validate.ts:186-192 collects exit refs (key is `soft`), validate.ts:251-277 — **soft key refs ALREADY get the full world resolver** (routes/areas.ts:73 + routes/world.ts:75 pass worldVnumIndex); the "key object not found" warnings the user sees are vnums NO area defines (stock draconia/hitower dangling keys). The 12b work is presentation: unresolvable → explicit INVALID flag, resolved → navigable link everywhere warnings render (PreviewPane.tsx warning list is plain text today).
- Map layout: /workspace/shattered-archive/apps/mud-builder-client/src/features/map/layout.ts DOOR_DELTAS currently parks U at (1,-1) and D at (-1,1) — those cells ARE the NE/SW diagonals, so U/D must move off the compass plane (short offset stubs or z-lanes) when diagonals arrive.
- Resets UI: apps/mud-builder-client/src/features/resets/ResetsPage.tsx renders a flat `resets.map(...)` list (line ~229); G/E/P rows have no visual tie to the M above; rows can't move as a group.
- Selects: areas.css:120 (.mb-field select), areas.css:332 (.mb-exit-row select), map.css:36 (.mb-map-picker select) style the closed control but native dropdown OPTIONs fall back to UA colors → near-invisible non-hover text on the dark theme. Fix = `color-scheme: dark` + explicit `option { background; color }` in a shared rule.
- Auth: apps/mud-builder-server/src/config.ts:42 `authDataPath = path.join(areaPath, 'backups')`; auth-store.ts — builder-auth.json holds plaintext `masterKey` + sha256-hashed child keys, loaded ONCE and cached (`this.data`), atomic persist 0600. audit.log also lives in backups/ (user asked only the auth key to move).
- World inspector: apps/mud-builder-server/src/routes/world.ts + client WorldPage — per-area errors/warnings/external today; no limit-pressure data.

## Steps
### [x] 1. (CLAUDE) Engine: 10-direction compass rose (C, additive)
- Do: extend merc-mud to doors 0-9 (6=NE 7=NW 8=SE 9=SW): merc.h exit array size + DIR_* defines + rev_dir table; act_move.c dir_name/movement (do_northeast/northwest/southeast/southwest + interp.c command table entries with sensible abbreviations ne/nw/se/sw); db.c load_rooms door bound 951 → 0-9, 'D' reset bound 820 → 0-9, 'R' bound 838 → 0-10; act_info.c autoexits, comm.c prompt dirs, scan.c. Grep ALL `< 6`/`> 5`/`[6]` door loops (fight.c, mob_prog.c, etc.) — every per-door iteration must cover 10. NO behavior change for doors 0-5.
- Files: /workspace/merc-mud/2.4/src/merc.h, act_move.c, act_info.c, comm.c, scan.c, db.c, interp.c (+ any per-door loop the grep finds)
- Verify: engine `make` clean; boot the stock corpus in the experimental game container's build stage (NOT the live one) — zero new bug() lines, a hand-made 2-room test area with an NE exit walks both ways. Do NOT recreate the live game container yet (that is step 7).
### [x] 2. (CLAUDE) Model + editors: door 0-9 through merc-area and the builder UI
- Do: RoomExit door widens to 0-9 (types.ts), parser/emitter accept-emit doors 6-9, validate.ts mirrors the new db.c bounds ('D' reset door 0-9, 'R' 0-10) and door-name labels gain NE/NW/SE/SW everywhere the client names doors (RoomEditor exit editor, ResetsPage DOOR_LABELS, map DOOR_NAMES). Map layout.ts: DOOR_DELTAS gains the DslMapper diagonals NE(1,-1) NW(-1,-1) SE(1,1) SW(-1,1); U/D move OFF the compass plane to short offset stubs beside the room node (they no longer claim grid cells). Canonical-form byte-identity for stock areas (no doors >5) must hold.
- Files: /workspace/shattered-archive/services/merc-area/src/types.ts, parse.ts, emit.ts, validate.ts (+tests); apps/mud-builder-client/src/features/areas/RoomEditor.tsx, features/resets/ResetsPage.tsx, features/map/layout.ts, MapPage.tsx (+tests)
- Verify: host merc-area + server + client suites green, tsc clean; REMEMBER `pnpm --filter @shatteredarchive/merc-area build` before server/client work; a fixture area with an NE exit round-trips parse→emit byte-identical and lays out diagonally.
### [x] 3. (CLAUDE) Teleports + blocked-path authoring (room scripts)
- Do: extend the mob_prog-based script engine with ROOM-attached triggers (entry trigger first) and a `warp <room-vnum>` action — DslMapper's Warp/Interaction is the semantic reference; #SCRIPTS grows a room variant (parser/emitter/validate collect the room + target-room refs); builder Scripts tab authors room warp scripts with preview; hot-load path reuses the Phase 2 mechanism (verify room scripts hot-load like mob scripts, else document copyover-only). Door-blocked paths need NO new engine work (locks/key already exist) — the authoring gap is UI: RoomEditor exit editor exposes locks/key clearly (door/pickproof/nopass states per db.c:965-977). Dig/lever-style verbs are OUT of 12b scope (log as a Phase 14 candidate) — the room-trigger foundation built here is their prerequisite.
- Files: /workspace/merc-mud/2.4/src/mob_prog.c, mob_prog.h, db.c, area_reload.c; /workspace/shattered-archive/services/merc-area/src (scripts model +tests); apps/mud-builder-client/src/features/scripts/ScriptEditor.tsx, features/areas/RoomEditor.tsx (+tests)
- Verify: engine make clean + test area: entering the room warps the walker; host suites green; validate flags a warp to an undefined room.
### [x] 4. (CLAUDE) Map render fidelity: one-way / loop-back / asymmetric / blocked / teleport
- Do: layout+render classify each exit using the model (reverseDir semantics from DslMapper dir.ts): reciprocal pair = plain edge; no reverse exit = ONE-WAY (arrowhead); toVnum === own vnum = LOOP-BACK (self-loop glyph); reverse exists but targets a different room = NON-RETURNING (arrowhead + distinct dash); locks>0 = door marker on the edge midpoint (locked/pickproof variants); room warp scripts render as dashed teleport edges to their target (portal-stub style when cross-area). Add a map legend. World map unchanged.
- Files: /workspace/shattered-archive/apps/mud-builder-client/src/features/map/layout.ts, MapPage.tsx, map.css (+tests: one fixture per classification)
- Verify: client suite green; fixture area exercising all five classifications renders each marker (test asserts classes/arrowheads).
### [x] 5. (CLAUDE) Builder UX batch: grouped resets, world-limit flags, select contrast, INVALID refs
- Do: (a) ResetsPage: group each M with its following G/E rows (and P rows chained to those objects) as a visual unit — colored left-border/badge per mob group cycling a palette where adjacent groups never share a color, indent members under the M, and move the WHOLE group up/down as one unit; ungrouped rows stay exactly as today, uncolored. (b) World inspector: per-vnum limit pressure from the db.c semantics in Context (spawn references vs declared limit; `-1`/`>50` handled) → flag "at/near limit — further resets will mostly not fire" on the area cards and a per-item list. (c) Shared select fix: `color-scheme: dark` + explicit `select option { background:#1f2430; color:<fg> }` rule covering .mb-field/.mb-exit-row/.mb-map-picker and any bare select. (d) Refs presentation: warnings that the resolver PROVED undefined anywhere render as flagged INVALID (distinct style + wording "no area defines this vnum"), resolved external refs render as navigable links in every warnings surface (PreviewPane already links external; extend the same to world inspector output).
- Files: /workspace/shattered-archive/apps/mud-builder-client/src/features/resets/ResetsPage.tsx (+test), features/world/* (+server routes/world.ts for limit data, +tests), features/areas/areas.css, features/map/map.css, features/areas/PreviewPane.tsx
- Verify: host suites green; reset-group test: E after M moves with the M; limit fixture flags an over-referenced object; INVALID styling asserted.
### [x] 6. (CLAUDE) Auth: auth/ folder + generate-master-key.sh rotation
- Do: config.ts authDataPath → `<area>/auth` (audit.log STAYS in backups/ — only the auth key moves); AuthStore boot migration: if auth/builder-auth.json missing and backups/builder-auth.json exists, move it (atomic rename, log once); AuthStore re-reads the file when its mtime changes (so external rotation applies without restart, cache stays for the hot path). New scripts/generate-master-key.sh (repo root scripts/): `[--key <value>]` (default crypto-random base64url), rewrites auth/builder-auth.json with the new masterKey and **keys: []** (all child keys require reprovisioning — the user's stated intent), 0600, atomic tmp+rename, prints the key ONCE to stdout and never writes it anywhere tracked. Root package.json gains `"generate-master-key": "bash scripts/generate-master-key.sh"` so `pnpm generate-master-key [-- --key <value>]` is the documented entry point (args pass through; script resolves the auth dir itself — from MERC_MUD_PATH or an explicit `--auth-dir`, so the pnpm form works without cd). Update docs (README access section) + compose notes if the bind path is referenced.
- Files: /workspace/shattered-archive/apps/mud-builder-server/src/config.ts, auth-store.ts (+tests: migration, mtime reload, rotation wipes keys), scripts/generate-master-key.sh (new), package.json (root), docs/mud-builder/README.md
- Verify: server suite green; manual: `pnpm generate-master-key -- --auth-dir <temp dir>` rotates, old child key 401s, new master 200s without restart.
### [x] 7. (CLAUDE) Deploy + live E2E + docs + sign-off
- Do: deploy engine (game compose recreate — the ONE sanctioned brief downtime; verify players-side boot log clean) + builder pair rebuild; live E2E driver: NE walk in a test area via the game, /api/map renders doors 6-9 + classification markers, room warp fires, select contrast spot-check, resets group render, rotation script E2E on the live bind mount (rotate → old keys dead → reprovision), auth file now under auth/, audit intact in backups/, stock corpus boot has zero new bug() lines. Update docs/mud-builder/README.md (Phase 12b section + Scope), refresh .annotated/.ai-context touched dirs, mark plan COMPLETE, re-set the parked Phase 13 doc (reset simulator) to ACTIVE.
- Files: (driver in scratchpad), /workspace/shattered-archive/docs/mud-builder/README.md, .ai-plans/20260717-1615-mud-builder-phase13-reset-simulator.md
- Verify: all driver checks pass; suites green; the ONLY game restart this phase is the step-7 engine recreate.

## Progress log

- 2026-07-17T18:00:00Z plan created from the user's Phase 12b directive (mapping corrections + UX/auth batch), displacing Phase 13 (parked, re-activate after). Pre-flight findings baked into Context: (1) vanilla Merc 2.4 is hard-6-dir with a boot-fatal door>5 check — the rose expansion is engine C work, explicitly sanctioned; numbering fixed at 6=NE 7=NW 8=SE 9=SW; (2) stock Merc has no teleport mechanism — room-script warp chosen (DslMapper Warp as reference) over a room flag because it reuses the Phase 2 hot-load path and is the prerequisite for future dig/lever verbs; (3) IMPORTANT correction to the prompt's premise: exit-KEY refs ALREADY get the full world-index resolution (validate.ts:266-275 soft refs hit resolveExternal; both preview and world routes pass the resolver) — the "key object not found" warnings are vnums NO area defines (stock draconia/hitower dangling keys), so the work is presentation (INVALID flag + links), not a missing search; (4) map layout's U/D deltas currently occupy the NE/SW diagonal cells and must move to stubs; (5) reset limits are statically computable (db.c:1301, 1380-1385, 1473-1480) for the world-limit flags.
- 2026-07-17T18:20:00Z step 6 amended per user: root package.json gets a `generate-master-key` script wrapping scripts/generate-master-key.sh (pnpm entry point, args pass through, script self-resolves the auth dir via MERC_MUD_PATH or --auth-dir).
- 2026-07-17T22:35:00Z step 1 done: engine rose — merc.h DIR_NORTHEAST..SOUTHWEST 6-9 + MAX_DIR 10, exit/old_exit arrays widened; act_move dir_name/rev_dir {…,9,8,7,6}, move_char bound, 4 do_* movers, find_door diagonals; interp.c gains ne/nw/se/sw SHORT entries (caught live: "ne" is NOT a prefix of "northeast" — it matched "news", "sw" matched social "swoon") + full names; comm.c prompt %e dirs, db.c (D reset bound, R bound MAX_DIR, load_rooms door bound, area/room loops, number_door 16-mask), update.c wander, fight.c flee loop, act_info look-dir + autoexits, act_wiz, scan.c (spell-only in this fork), area_reload.c SEXIT[MAX_DIR] + loops + D/R validation bounds. VERIFIED in ephemeral container (live game untouched): compile clean; stock corpus boots with ONLY the two Fix_exits lines the LIVE engine also logs every boot (3458:2, 8705:4 — pre-existing data asymmetries, baselined via docker logs merc-mud2.4); scripted telnet char walked school→ne→32100→northeast→32101→sw→southwest→school (test area + school D6 edit existed only inside the ephemeral container).
- 2026-07-18T02:00:00Z post-completion fix (user report): `pnpm generate-master-key` failed on the host because PATH `bash` is the WSL relay stub with no distro behind it (execvpe(/bin/bash) ENOENT). Rotation logic moved to pure Node (scripts/generate-master-key.cjs — same flags/validation/atomic 0600 write), pnpm entry now `node scripts/generate-master-key.cjs`, and generate-master-key.sh became a thin exec-node wrapper for POSIX shells. Verified from PowerShell (designated + random key) and via the .sh wrapper.
- 2026-07-18T01:35:00Z step 7 done + plan COMPLETE: deployed the engine (game compose rebuild+recreate — the ONE sanctioned downtime; new binary boots the stock corpus with ONLY the two baselined Fix_exits lines) and the builder pair; the builder's boot log PROVED the live auth migration ("[auth] migrated /mud/area/backups/builder-auth.json -> /mud/area/auth/builder-auth.json", host confirms). Live E2E via the edge vhost: 19/19 PASS — caps/auth guard (anon 401, master 200), import PREVIEW of a D6/D9 + R-warp area (no errors, canonical keeps D6 + "R 32101 entry", warp 3001 resolved cross-area to midgaard's Temple, nothing written), midgaard map 143 rooms all exits carrying locks with stock doors present and no stock warps, world map 53 areas/109 links, world inspector 53 areas each with limitPressure (44 REAL flags on stock, e.g. ofcol2 obj#616 demand 14 vs limit 6) and zero errors, audit viewer master-only with audit.log still in backups/ holding no token values. Then the live ROTATION pass: pnpm generate-master-key against the live auth dir → old master 401, new master 200, child keys wiped (both were already-revoked phase9 test keys), builder container NOT restarted (StartedAt unchanged). NOTE: node fetch drops a manual Host header, so the driver targets the hosts-file vhost directly. Docs: docs/mud-builder README (Phase 12b section, Phase 9 auth/ + rotation, Scope) + NEW merc-mud/2.4/doc/README.md (fork feature reference: rose, #SCRIPTS M+R, reload plumbing, area subdirs); .annotated refreshed across merc-mud src/doc, merc-area, server src/routes, client map/resets/world/scripts/api, docs. Final suites: merc-area 74/74, server 73/73, client 92/92, tsc clean everywhere. Phase 13 (reset simulator) re-set to ACTIVE. auth rework — config splits authDataPath (now `<area>/auth`) from auditDataPath (audit.log STAYS in backups/); AuthStore migrates a legacy backups/builder-auth.json by atomic rename on first load and re-reads the file on mtime change (external rotation applies with NO restart; persist updates the cached mtime); scripts/generate-master-key.sh (`--key`/`--auth-dir`, URL-safe key guard, atomic tmp+rename, 0600, prints the key once, ALWAYS writes keys:[] so every child key needs reprovisioning) + root package.json `generate-master-key` pnpm entry; merc-mud .gitignore comment updated + auth/ dir ignored. Tests: legacy migration (old master keeps working from auth/), rotation matrix (old master 401, old child key 401, rotated master 200 — no restart). Script exercised against a temp dir (designated + random key). server 73/73, tsc clean.
- 2026-07-18T00:15:00Z step 5 done: UX batch — (a) ResetsPage computeBlocks partitions M + consecutive G/E/P riders into colored move-as-a-unit blocks (6-color palette cycled sequentially so adjacent groups differ; lone rows stay unstyled), anchors move the whole block, members keep row-swap, colored left border + member indent/tint CSS; tests: anchor/member class counts + block move past an O. (b) /api/world gains per-area `limitPressure` (world-wide two-pass: spawn demand per mob/object vnum from M and G/E/P resets vs the tightest declared limit with db.c decoding >50→6, -1→999; flagged only when demand > limit < 999, surfaced on the DEFINING area); WorldPage shows ⚖ flags + totals; test: pressured fixture (2 M / 2 E vs limit 1) flags mob+object, unpressured stays empty. (c) select contrast ROOT CAUSE was index.css `color-scheme: light dark` — a light-mode OS painted the native dropdown popup white under light text; now `dark` + explicit select/option colors. (d) resolver-proven-undefined refs render as ✖ INVALID (PreviewPane + WorldPage, red bold, wording says the whole world was searched); resolved external refs were already navigable links on both surfaces. server 71/71, client 92/92, tsc clean.
- 2026-07-17T23:45:00Z step 4 done: map fidelity — server /api/map/:file exits gain `locks`, rooms gain `warps` (R-script `warp <vnum>` targets, world-resolved external like exits; test: locked-door + cross-area/internal warp fixture, server 70/70); client layout.ts REV_DIR + per-internal-edge classification (two-way | one-way = no reverse-door exit | non-returning = reverse leads elsewhere | loop = self), warp edges (door -1, always directed) + warp portal stubs (`vnum:w:target`); MapPage renders arrowheads (SVG markers, tip pulled clear of the node), dashed red non-returning, dotted violet warp, self-loop ring, rotated amber door marker at edge midpoint w/ lock-state title, and an HTML legend; map.css palette. Tests: classification matrix + warp/locks layout units, MapPage fidelity render (classes+door title+legend). client 90/90, tsc clean.
- 2026-07-17T23:20:00Z step 3 done: room-script warp teleports — engine: ROOM_INDEX_DATA.rprogs + RTRIG_ENTRY vocabulary, rp_run interpreter (echo/echoroom/warp, MP_MAX_LINES budget, unknown-cmd logged no-op, warp never re-triggers = no chain teleports), rp_entry_trigger wired into move_char AFTER the mob-trigger guard (walk-in only; goto/transfer/summon placements don't fire; leader-warp drops followers by design), db.c load_scripts + load_rooms rprogs init, area_reload SSCRIPT.is_room staging/validation ("room script references room N not defined in this file")/commit (rooms wholesale-replace rprogs before commit_scripts reattaches, order rooms→mobs→scripts verified at line 2113). VERIFIED: --mp-test now 9 checks PASS (rp lookup + entry warp via limbo), ephemeral-container E2E: char walked ne ne, R-script fired ("A magical vortex seizes you!") and warped to Temple 3001. TS: MobScript.attach 'mob'|'room' (absent = mob, mobVnum holds the attachment vnum), parse/emit R lines, validateScripts room branch (ROOM_SCRIPT_TRIGGERS=['entry'], same-file room, NEW section-order error: #SCRIPTS with room entries must follow #ROOMS or boot exits), collectRefs adds room-script refs + `warp <vnum>` body targets as room refs (cross-area resolvable). Client: ScriptEditor room variant (room picker, entry-only triggers, room vocabulary help), ScriptsPage "+ Add room script". Door/lock authoring needed NO work — RoomEditor already exposes all 5 db.c lock states + key. merc-area 74/74, client 87/87 tsc clean, server 69/69.
- 2026-07-17T22:55:00Z step 2 done: model+editors — merc-area parse/emit door bound 0-9, RoomExit doc, NEW validateRefs D/R reset bound errors mirroring db.c exit(1) (D door 0-9 + locks 0-2, R count ≤10; stock corpus sweep still clean); client flags.ts DOOR_NAMES + ResetsPage DOOR_LABELS + map layout DOOR_NAMES gain the four diagonals; layout DOOR_DELTAS: NE/NW/SE/SW take the corner cells, U/D become [0,0] sentinels ring-placed near the source (findFreeCell skips the probe on zero delta). Tests: rose.are D6/D9 byte-identical round trip + D10 rejection, D/R bound matrix, diagonal corner-cell layout + U ring-placement. merc-area 71/71 (+5), client 86/86 (+2) tsc clean, server 69/69 against rebuilt dist.
