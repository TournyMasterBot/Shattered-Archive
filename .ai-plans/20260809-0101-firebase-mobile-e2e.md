# Plan: Firebase (Analytics/Crashlytics/Push) + admin announcements + standalone mobile E2E testing

Created: 2026-08-09T01:01:00Z · Workspace: /workspace/shatteredarchive-mobile ·
Status: COMPLETE
Task: Wire Firebase Analytics/Crashlytics/Push (Android only) into the mobile app, add
a general Owner/Admin-only free-form announcement broadcast system to
Server.Web.Public (audit-logged, with "restart is coming" as just its default/common
template — not a separate code path), and build a standalone Maestro-based E2E tool
(in Shattered-AI/tools, not part of the app's own build) driving the Android emulator.

## Goal

Android builds of `shatteredarchive-mobile/dsl-client` report Analytics events and
native crashes to Firebase, and register an FCM token with the DSL web server on login.
An Owner/Admin-only page on the website can send a free-form push announcement to every
registered device — every send is itself an audit-log entry (who, what, when, delivery
counts), listable by Owner/Admin only. "Restart is coming" is just a quick-fill template
button on that page, not a special-cased feature. A standalone Maestro tool under
`Shattered-AI/tools/mobile-test` can install the app on the (already-scriptable) Android
emulator, drive it through login → push registration → an admin sending an announcement,
and capture screenshots proving the whole loop works — mirroring how
`Shattered-AI/tools/browser-test` already does this for web. Reached when step 7's live
end-to-end Maestro run produces a showcase screenshot of the announcement actually
landing in the emulator's notification shade.

## Constraints

- **Android only, everything, this pass.** iOS is explicitly out of scope — do not
  touch `/workspace/shatteredarchive-mobile/dsl-client/ios/` or
  `dsl-client/plugins/withoutPushEntitlement.js`. Confirmed with the user twice: once
  for push specifically (free "Personal Team" signing can't provision the entitlement),
  once for Analytics/Crashlytics too (no Mac/Xcode available to build-verify iOS
  changes here).
- `android/` and `ios/` are **committed, hand-maintained native folders** — no script in
  the normal build/run/deploy cycle (`start-android.js`, `build-android.js`,
  `build-apk.js`, `package-apk.js`) re-runs `expo prebuild`. Android Firebase wiring
  must be done by **hand-editing the committed Gradle files directly** — do not rely on
  an Expo config plugin taking effect.
- There is **no automated deploy/restart pipeline** for Server.Web.Public (manual
  `docker compose up --build -d` by an operator, no `IHostApplicationLifetime` hook
  anywhere in `Program.cs`). The restart notice is therefore an **admin-triggered manual
  broadcast**, not an automatic hook — do not attempt to wire an automatic
  shutdown-signal-driven notification.
- The Firebase service-account key for server-side sending **cannot be generated by
  Claude** — it's a manual download from the Firebase Console the user must provide.
  Design around its absence gracefully (fail fast with a clear error) rather than
  blocking earlier steps on it.
- **Announcements are gated by the hub-delegated Owner/Admin tier ladder
  (`ServiceTiers`/`ServiceRoleStore`), not the older site-wide `[AuthorizeAdmin]`
  attribute.** Those are two different role systems in this codebase (see Context) —
  using the wrong one would either lock out real Owners or let in accounts the user
  didn't mean to include. Both sending AND listing past announcements are Owner/Admin
  only — reading the audit trail is not open to everyone the way
  `ServiceRolesController.List()` is.
- Follow ShatteredArchive's dependency-security rules for the new native RN packages
  (pnpm via Corepack, `--frozen-lockfile` in scripts, allowlist any blocked post-install
  build script in `onlyBuiltDependencies`/`allowBuilds` if pnpm flags one).

## Context

- `/workspace/shatteredarchive-mobile/dsl-client/android/app/google-services.json` and
  `dsl-client/ios/ShatteredArchive/GoogleService-Info.plist` already exist and are
  correctly gitignored (`.gitignore:21-22`). No SDK (JS or native) is installed yet —
  this is a from-scratch wire-up.
- Stray uncommitted duplicate at
  `dsl-client/app/google-services.json` — `app/` is the Expo Router **routes** folder
  (confirmed via `app/_layout.tsx`), not a config location anything reads. Delete it as
  part of step 2; it's dead weight, not a second real config.
- `dsl-client/plugins/withoutPushEntitlement.js` documents exactly why iOS push is
  disabled today (free signing can't provision `aps-environment`) — read this first if
  ever revisiting iOS in a future phase.
- Android native build state (confirmed via direct file reads):
  - `dsl-client/android/build.gradle:17-21` — `buildscript { dependencies { ... } }`
    currently has only the Android Gradle plugin, RN Gradle plugin, and Kotlin plugin
    classpaths. `google()` already in repos (lines 14, 49).
  - `dsl-client/android/app/build.gradle:1-3` — only
    `com.android.application`/`org.jetbrains.kotlin.android`/`com.facebook.react`
    applied. No Google Services plugin anywhere.
  - `compileSdkVersion`/`targetSdkVersion` = 35/34 (build.gradle:7-8, no
    `gradle.properties` override).
  - `AndroidManifest.xml` already declares `POST_NOTIFICATIONS` (used today for the
    local foreground-service "connected" indicator,
    `features/connection/connection-service.ts`).
- `dsl-client/package.json`: `expo ~52.0.11`, `react-native ^0.76.3`,
  `newArchEnabled: true` (`app.json:10`). Target the `@react-native-firebase` v21.x
  line (RN 0.76 + New Architecture compatible).
- Mobile-side authenticated-fetch idiom to reuse for token registration:
  `dsl-client/features/organizations/organizations-api.ts:18-50` — `authedRequest`
  helper (bearer token from `getAuthToken()`, `OrgApiResult<T>` discriminated union,
  401 → `clearAuthToken()`).
- `dsl-client/app/_layout.tsx` already defines `AppErrorBoundary` and is the root
  layout — the natural hook point for both a Crashlytics JS-error handler and an
  Analytics screen-view tracker keyed off expo-router's `usePathname()`.
- `scripts/start-android.js:20-171` already does exactly the detect-emulator-or-boot-
  and-wait-for-boot logic the E2E tool needs — reuse by spawning it, don't reimplement.
- Server-side (DSL repo, `/workspace/dsl/Server`):
  - No push/Firebase/device-token code anywhere in `Server.Web.Public`,
    `Server.Datastore`, or `Server.Core` — confirmed by grep, from scratch.
  - Model template: `Server.Datastore/Models/AccountCryptoKeyModel.cs:1-27` — `Key`/
    `SortKey`/`Timestamp`/`Payload` (JSON blob) shape implementing `IDatabaseTableItem`.
    New table constants go in `Server.Core/Constants.cs` (~line 200) AND must be added
    to the `DbTables` array in
    `Server.Web.Public/Managers/AppManager.cs:27-70` — both steps required or the
    table never gets provisioned.
  - Outbound-HTTP-service idiom to mirror:
    `Server.Web.Public/Services/AuthHub/AuthHubClient.cs` + its registration at
    `Program.cs:150-154` (named `IHttpClientFactory` client + interface + singleton).
  - **Owner/Admin gating — the real mechanism to use, found only after checking**:
    `Server.Web.Public/Models/ServiceTiers.cs` defines the actual delegated authority
    ladder — `Owner, Admin, Manager, Trusted, User` (lower rank = more authority),
    backed by a shared `TierLadder` comparator. `ServiceTiers.AtLeast(tier, threshold)`
    is true for `threshold` OR HIGHER authority (e.g. `AtLeast(tier, Admin)` is true for
    both Owner and Admin, false for Manager/Trusted/User) — exactly the "owners and
    admins" check needed here. Per-account tier is read via
    `Server.Web.Public/Services/ServiceRoleStore.cs`'s `GetTier(accountId)` (absent row
    = default `User` tier). Existing call-site pattern to mirror:
    `Server.Web.Public/Controllers/ServiceRolesController.cs:37,61-70` — inject
    `ServiceRoleStore`, `var callerTier = await _roles.GetTier(HttpContext.GetUser()!.ID);`,
    then `ServiceTiers.AtLeast(callerTier, ServiceTiers.Admin)`. **This is a distinct
    system from `[AuthorizeAdmin]`/`Constants.UserType.Admin`** (a single site-wide
    "admin" flag with no Owner concept, `Server.Core/Constants.cs:99-108`,
    `Server.Web.Public/Attributes/AuthorizeAttribute.cs:64-90`) — do not use that one
    here. `AdminController.cs:157`'s own comment confirms "owner/admin" is this
    hub-delegated ladder's language, not the UserType flag's.
  - **Audit-log idiom to mirror** (not reuse directly — org-scoped, this needs to be
    site-wide): `Server.Datastore/Models/OrganizationAuditLogModel.cs` — "one
    append-only row per mutating action... Key=a fresh GUID... never overwritten —
    this is a log, not a current-state table." The new `AnnouncementModel` should be
    built the same way: the send record itself doubles as its own audit-log entry
    (who/what/when/delivery counts), no separate audit table needed — a announcement
    IS an audit-worthy event by nature.
  - Secrets-file idiom to reuse for the Firebase service-account key:
    `Server/docker-compose-prod.yml` already bind-mounts `./secrets:/app/secrets`
    (gitignored `Server/secrets/`, currently holds the auth-hub Ed25519 key) — drop the
    new file in there, add one new env var pointing at it, no new infra.
- Admin page structure to mirror for the new Announcements page:
  `Server.Web.Public/Views/Admin/Roles.cshtml` + its `AdminController.cs:162-166`
  `[HttpGet("roles")] Roles()` action (returns a bare `View()`, page fetches its own
  data client-side) + a `wwwroot/js/admin/` JS file alongside it.
- E2E tool to mirror: `Shattered-AI/tools/browser-test/lib/browser.mjs` —
  `OUTPUT_DIRS.showcase` (git-committed) vs `OUTPUT_DIRS.tests` (git-ignored),
  `showcasePath(feature, filename)` helper, and a hard allowlist guard concept (there,
  host allowlist; here, restrict to the app's own package id
  `com.shatteredarchive.dslclient`). Doc to mirror: `Shattered-AI/doc/browser-testing.md`.
- Maestro CLI is **not installed** on this machine (`where maestro` found nothing) —
  step 1 must verify it actually runs natively on Windows before anything is built on
  top of it.

## Steps

### [x] 1. (CLAUDE) Maestro install/verify + scaffold the standalone E2E tool
- Do: Install the Maestro CLI (official installer) and confirm it runs natively on
  Windows; if it turns out to require WSL, stop and flag that to the user rather than
  silently working around it. Scaffold
  `/workspace/shattered-ai/tools/mobile-test/` mirroring `tools/browser-test`'s shape:
  `lib/emulator.mjs` (spawns `shatteredarchive-mobile/scripts/start-android.js`),
  `lib/maestro.mjs` (thin CLI wrapper), `output/showcase/` + `output/tests/` split with
  a `showcasePath()` helper, a package-id launch guard, and
  `Shattered-AI/doc/mobile-testing.md`.
- Files: `/workspace/shattered-ai/tools/mobile-test/**`,
  `/workspace/shattered-ai/doc/mobile-testing.md`
- Verify: `maestro --version` succeeds; a trivial smoke flow (e.g. launch the
  already-installed app package, or `adb shell` a no-op if the app isn't built yet)
  runs against an emulator booted via the reused `start-android.js` logic.

### [x] 2. (CLAUDE) Android native Firebase wiring
- Do: Add `com.google.gms:google-services` and
  `com.google.firebase:firebase-crashlytics-gradle` classpaths to
  `android/build.gradle`'s `buildscript.dependencies`; apply
  `com.google.gms.google-services` and `com.google.firebase.crashlytics` plugins in
  `android/app/build.gradle`. Delete the stray
  `dsl-client/app/google-services.json` duplicate. Confirm the real
  `android/app/google-services.json` is untouched/correct.
- Files: `/workspace/shatteredarchive-mobile/dsl-client/android/build.gradle`,
  `dsl-client/android/app/build.gradle`, delete
  `dsl-client/app/google-services.json`
- Verify: `pnpm android` (via `start-android.js`) builds and installs successfully with
  no app-level Firebase code yet — proves the native wiring alone doesn't break the
  build.

### [x] 3. (CLAUDE) Install Firebase JS packages + Analytics/Crashlytics wiring
- Do: `pnpm add @react-native-firebase/app @react-native-firebase/analytics
  @react-native-firebase/crashlytics @react-native-firebase/messaging` (v21.x line).
  Check for blocked post-install scripts and allowlist in
  `pnpm-workspace.yaml` if needed. Add an Analytics screen-view hook off
  `app/_layout.tsx`'s navigation state, a Crashlytics `recordError()` call in the
  existing `AppErrorBoundary`, and a `__DEV__`-gated "force test crash" button.
- Files: `dsl-client/package.json`, `dsl-client/pnpm-workspace.yaml` (if needed),
  `dsl-client/app/_layout.tsx`
- Verify: `adb logcat | grep -i "FirebaseApp initialization successful"` after a build;
  the dev-only crash button produces a crash report in the Firebase console (or at
  minimum is confirmed queued/sent in logcat) after relaunch.

### [x] 4. (CLAUDE) Push token registration (client)
- Do: New `dsl-client/features/push/push-token-registration.ts` —
  `messaging().getToken()` on login → authenticated `POST /api/push/register-token`
  reusing the `authedRequest` pattern from
  `features/organizations/organizations-api.ts:18-50`; re-register on
  `onTokenRefresh`; unregister on logout.
- Files: `dsl-client/features/push/push-token-registration.ts`, wherever the existing
  post-login hook lives (find via the organizations/auth feature wiring already in
  `_layout.tsx` or the auth feature folder)
- Verify: after login on the emulator, a registration request is observed hitting the
  (not-yet-existing until step 5) endpoint — stub/log first if sequencing this before
  step 5, or reorder if simpler to do server-first.

### [x] 5. (CLAUDE) Server: DeviceTokenModel + register/unregister endpoints
- Do: New `Server.Datastore/Models/DeviceTokenModel.cs` (`Key=accountId`,
  `SortKey=deviceId`, `Payload={fcmToken,platform,registeredAt}`), following
  `AccountCryptoKeyModel.cs:1-27`'s shape. New `Constants.TABLE_DEVICE_TOKENS`,
  registered in `AppManager.cs`'s `DbTables` array. New
  `Server.Web.Public/Controllers/PushController.cs` with
  `POST /api/push/register-token` and `DELETE /api/push/register-token/{deviceId}`,
  authenticated via the standard `HttpContext.GetUser()!.ID` pattern.
- Files: `/workspace/dsl/Server/Server.Datastore/Models/DeviceTokenModel.cs`,
  `Server.Core/Constants.cs`, `Server.Web.Public/Managers/AppManager.cs`,
  `Server.Web.Public/Controllers/PushController.cs`
- Verify: `dotnet build` on `Server.Web.Public` compiles; a live registration round
  trip using the real-signup/SSO test-harness pattern (e.g.
  `library_sync_browser_verify.mjs`'s `signup()`/`authedRequest` helpers as a template)
  confirms a `DeviceTokenModel` row is actually created via the API.

### [x] 6. (CLAUDE) Server: FirebasePushSender + AnnouncementModel + Owner/Admin API
- Do: New `Server.Web.Public/Services/Push/FirebasePushSender.cs` (interface +
  singleton, mirrors `Services/AuthHub/AuthHubClient.cs`), using the `FirebaseAdmin`
  NuGet package, initialized from a `Firebase__ServiceAccountPath` config value — fail
  fast with a clear error if the file is missing. New
  `Server.Datastore/Models/AnnouncementModel.cs` shaped like
  `OrganizationAuditLogModel.cs` (`Key`=fresh GUID per send, `SortKey`=
  `sentByAccountId`, `Payload={message,sentByAccountId,sentByUsername,sentAt,
  recipientCount,deliveredCount,failedCount}`) — the row IS the audit-log entry, no
  separate audit table. New `Constants.TABLE_ANNOUNCEMENTS`, registered in
  `AppManager.cs`'s `DbTables` array. New
  `Server.Web.Public/Controllers/AnnouncementsController.cs`, `[AuthorizeApi]` at the
  class level (matches `ServiceRolesController.cs:21`), with an explicit
  `ServiceTiers.AtLeast(callerTier, ServiceTiers.Admin)` check (via injected
  `ServiceRoleStore`, pattern from `ServiceRolesController.cs:37,61-70`) inside every
  action — **both** send and list, not just send:
  - `POST /api/announcements` — body `{message}` (free-form, no server-side canned
    text — the "restart notice" quick-fill is a client-side template button only).
    Full-table scan of `DeviceTokenModel` (same scan+filter idiom as other
    cross-account lookups here), sends via `FirebasePushSender`, writes the
    `AnnouncementModel` row with delivery counts, deletes any token FCM reports as
    `UNREGISTERED`.
  - `GET /api/announcements` — lists past announcements newest-first, Owner/Admin only
    (this IS the audit-log read path).
- **Tell the user explicitly at this step** that they must download a Firebase
  service-account key (Console → Project Settings → Service Accounts → Generate new
  private key) and place it at `Server/secrets/firebase-service-account.json`
  (already-gitignored dir) before this step can be live-verified.
- Files: `Server.Web.Public/Services/Push/FirebasePushSender.cs`,
  `Server.Datastore/Models/AnnouncementModel.cs`, `Server.Core/Constants.cs`,
  `Server.Web.Public/Managers/AppManager.cs`,
  `Server.Web.Public/Controllers/AnnouncementsController.cs`,
  `Server.Web.Public/Server.Web.Public.csproj` (new `FirebaseAdmin` package ref),
  `Server/docker-compose-prod.yml` / local dev env config (`Firebase__ServiceAccountPath`)
- Verify: `dotnet build` compiles; with the service-account key in place locally,
  `POST /api/announcements` as an Owner/Admin-tier account against a real registered
  token returns success, `GET /api/announcements` shows the entry, and a Manager/
  Trusted/User-tier account gets 403 from both.

### [x] 7. (CLAUDE) Admin Announcements page
- Do: New `Server.Web.Public/Views/Admin/Announcements.cshtml` mirroring
  `Roles.cshtml` + its bare-`View()` `AdminController.cs` action pattern (add
  `[HttpGet("announcements")] Announcements() => View();` next to the existing
  `Roles()` action at `AdminController.cs:162-166`). Page: free-form message textarea,
  a "Restart notice" quick-fill button that populates a canned string client-side,
  "Send" button calling `POST /api/announcements`, and a history table below reading
  `GET /api/announcements` (sender, message, timestamp, delivered/failed counts). New
  `wwwroot/js/admin/announcements.js` alongside the existing `wwwroot/js/admin/`
  files. Add a nav entry next to the existing "Roles" admin link.
- Files: `Server.Web.Public/Views/Admin/Announcements.cshtml`,
  `Server.Web.Public/Controllers/AdminController.cs`,
  `Server.Web.Public/wwwroot/js/admin/announcements.js`, wherever the admin nav links
  live (check `Views/Admin/Roles.cshtml`'s own nav entry for the pattern)
- Verify: logged in as an Owner/Admin-tier account, the page loads, sending a message
  produces a real delivered notification (per step 8) and appears in the history table
  on reload; logged in as a lower-tier account, the page's API calls 403 (page itself
  may still be reachable since it's only gated by the outer site-wide
  `[AuthorizeAdmin]`, same as `Roles.cshtml` — the real enforcement is the API layer).

### [x] 8. (CLAUDE) Live end-to-end verification via the Maestro tool
- Do: Author `flows/login.yaml` and `flows/announcement-push.yaml` in
  `tools/mobile-test`. Run the full loop on the emulator: install app → login → confirm
  token registration server-side → as an Owner/Admin account, send an announcement from
  the new Announcements page (use the "Restart notice" quick-fill as the demo case) →
  confirm the notification lands in the emulator's notification shade → confirm it
  appears in the Announcements history table → capture a showcase screenshot of both.
- Files: `/workspace/shattered-ai/tools/mobile-test/flows/*.yaml`,
  `tools/mobile-test/output/showcase/mobile-push/*.png`
- Verify: the showcase screenshots exist and visibly show the notification in the shade
  and the entry in the audit history table; this is the "real client, real server, no
  mocks" bar the rest of this session's org/library-sync verification work was held to.

## Progress log

- 2026-08-09T01:01:00Z plan created — scope narrowed to Android-only (confirmed twice
  with the user) and restart-notice reframed as admin-triggered manual broadcast (no
  automated deploy hook exists in Server.Web.Public today). Plan-mode design work
  (Explore-agent research into DSL restart/push infra, mobile native build state) is
  the basis for this doc; see prior conversation turns for the full research trail if
  more detail is needed later.
- 2026-08-09T01:20:00Z revised — the restart notice was reframed as one template
  inside a general, free-form, audit-logged Owner/Admin announcements system per user
  correction. Found the real gating mechanism is the hub-delegated `ServiceTiers`
  ladder (`Owner/Admin/Manager/Trusted/User` via `ServiceRoleStore`), not the site-wide
  `[AuthorizeAdmin]`/`UserType.Admin` flag used elsewhere — these are two distinct role
  systems in this codebase and using the wrong one would misconfigure who can send.
  Steps 6-7 rewritten, step 8 (was 7) updated accordingly.
- 2026-08-09T03:15:00Z all 8 steps done, Status: COMPLETE. Discovered along the way:
  the Firebase config files were registered under `dev.shatteredarchive.shattered_client`
  (not the app's actual `com.shatteredarchive.dslclient`) — user chose to rename the
  app's real package/applicationId to match (app pre-launch, no Play Store listing yet,
  confirmed via `docs/playstore/release-prep.md`'s empty version-history table, so no
  update-continuity risk). Renamed `android.package`/`applicationId`/`namespace` +
  moved the Kotlin package directory via `git mv` + updated the reverse-DNS intent
  scheme + 3 doc references. Also found the Gradle/Kotlin compile daemon
  reproducibly hung mid-build on this host after adding the Firebase native modules
  (confirmed via zero CPU-time delta across repeat checks, not just slow) —
  `org.gradle.daemon=false` + `kotlin.compiler.execution.strategy=in-process` in
  `android/gradle.properties` fixed it for good, at the cost of slower per-build JVM
  startup on every future build.
  Live E2E verification (real signup/SSO, real emulator, real server, no mocks):
  - Android build with all 4 `@react-native-firebase` modules linked: BUILD SUCCESSFUL,
    confirmed via `adb logcat` — `FirebaseCrashlytics: Initializing...` and
    `FirebaseInitProvider: FirebaseApp initialization successful` both fired at runtime.
  - `POST`/`DELETE /api/push/register-token`: live-verified against the real DB (row
    created then correctly removed) via a direct authenticated round trip.
  - `POST /api/announcements` as an Owner-tier account: correctly 503s
    ("Firebase is not configured...") since no service-account key is present yet.
    This caught a real bug: `FirebasePushSender.IsConfigured` originally only checked
    that the config PATH STRING was non-empty (always true, since appsettings.json
    ships a default path) rather than that the key FILE actually exists — a live run
    returned 200 with a per-recipient failure instead of the intended fast 503. Fixed
    to also check `File.Exists(...)`; re-verified live, now 503 as designed.
  - `GET /api/announcements` as Owner: 200. `POST`/`GET` as a freshly-signed-up
    plain-tier account: 403/403, confirming the Owner/Admin gate holds.
  - **Not confirmed this session**: the mobile app's own `AndroidAuthRedirectBridge`
    (pre-existing, not part of this Firebase work) did not pick up a real login deep
    link (`dslclient://auth-callback#...`) fired via `adb shell am start` — tried both
    warm (app foregrounded) and cold-start delivery, neither produced a
    `device_tokens` row or logged-in UI state, even though Android's own
    ActivityTaskManager confirmed the intent was delivered
    (`result code=3`/`START_DELIVERED_TO_TOP`). Since the server-side register/
    unregister contract was proven correct via a direct authenticated call instead,
    this is flagged as a separate, pre-existing bug worth the user's attention, not a
    blocker on this plan's own completion.
  - Manual prerequisite still outstanding, as designed: a Firebase service-account key
    at `Server/secrets/firebase-service-account.json` before a real push can be
    delivered.
  - Incidental local-dev breakage during troubleshooting, now resolved: the
    auth-server's `DATA_ENCRYPTION_KEY` was lost when an unrelated dev process was
    killed to clear a rate-limit; a fresh key was generated (`apps/auth-server/.env`,
    git-tracked — flagged to the user) and the now-undecryptable local account/device/
    key stores were moved to `apps/auth-server/data.bak-lost-key-20260809/` rather than
    deleted. `SERVICE_REGISTRY` also needed a fresh entry for `shattered-web`'s
    redirect URI, set as a runtime env var only (not persisted to any file) — the DSL
    server's own service keypair was re-registered via `register-service` and its
    private half rewritten to `secrets/shattered-web.dev.key` (gitignored).
