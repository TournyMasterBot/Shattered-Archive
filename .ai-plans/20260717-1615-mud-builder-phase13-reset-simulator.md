# Plan: MUD Builder — Phase 13 (reset simulator: preview spawn state)

Created: 2026-07-17T16:15:00Z · Workspace: /workspace/shattered-archive · Status: COMPLETE
Task: Simulate an area's #RESETS so builders can SEE what actually spawns where — mobs with their equipped/carried objects, container contents, door states — before any reload, straight from the model.

## Goal
A "Simulate" view (Resets tab pane + per-room panel in the Areas preview + counts on the Map) shows the post-reset world state computed from the parsed model by a pure simulator in merc-area that mirrors db.c/reset_area semantics (M/O/P/G/E/D/R commands, global limits, LastMob/LastObj dependency chain). Done when the simulator reproduces db.c behavior on the stock corpus (spot-verified against known areas), all suites stay green, everything is read-only, and the MUD never restarts.

## Constraints
- STABILITY IS KING; no C changes; strictly read-only — the simulator consumes the parsed model, nothing touches disk or the game.
- Simulator semantics MUST mirror stock Merc db.c reset_area (the same file the game boots): M creates LastMob (obeying its limit), G/E give/equip to LastMob only when it was just created, O places obj in room (skip when area not empty per db.c rules — model the simple boot-state case: fresh boot, no players), P puts into LastObj-matching container, D sets door lock state both sides, R randomizes exits (report as "randomized", don't pick an order). Boot-state simulation only — repop drift (players, kills) is out of scope and said so in the UI.
- Cross-area object/mob vnums resolve via the existing worldVnumIndex (Phase 11); unresolvable vnums surface as simulation warnings, never crashes.
- All code testable in isolation; never-crash server contract; qwen (container) must NOT run pnpm install|build|test.
- qwen may transcribe/read but never verify with pnpm; JS build/test verification is a HOST task.

## Context
- Reset model: /workspace/shattered-archive/services/merc-area/src/types.ts (ResetsSection, Reset commands M/O/P/G/E/D/R + ResetComment) — parsed verbatim from the .are files.
- C semantics reference: /workspace/merc-mud/2.4/src/db.c reset_area (LastMob/LastObj state machine, limit checks) — READ ONLY, for fidelity.
- World index for cross-area vnums: /workspace/shattered-archive/apps/mud-builder-server/src/area-store.ts worldVnumIndex/resolveRefs (Phase 11, mtime-cached).
- Map tab (Phase 12): /workspace/shattered-archive/apps/mud-builder-client/src/features/map/ (MapPage/layout/WorldMap) — spawn badges hang off AreaMapRoom data.
- Resets tab: /workspace/shattered-archive/apps/mud-builder-client/src/features/resets/ResetsPage.tsx; Areas preview pane: src/features/areas/PreviewPane.tsx.
- Read-only aggregate idiom to copy: /workspace/shattered-archive/apps/mud-builder-server/src/routes/map.ts (Phase 12).

## Steps
### [x] 1. (CLAUDE) merc-area: pure reset simulator + tests
- Do: simulateResets(area, opts?: { resolveExternal }) → per-room spawn state: mobs [{vnum, name, count, equipped: [{slot, vnum, name}], carried: [...]}], objects [{vnum, name, contents: [...]}], doors [{room, door, state}], randomizedExits [rooms], warnings [] (unknown vnums, G/E with no live LastMob, P with no container — mirror db.c's silent skips as WARNINGS so builders see them). Follow db.c reset_area exactly for boot state (fresh, empty rooms). Unit tests: M+E+G chain, M limit honored, O placement, P into container, D lock states, R reported, orphan G/E warning, cross-area object via resolver.
- Files: /workspace/shattered-archive/services/merc-area/src/simulate.ts (new), src/simulate.test.ts (new), src/index.ts (exports)
- Verify: host merc-area suite green; tsc clean. REMEMBER: pnpm --filter @shatteredarchive/merc-area build before server work (server resolves the built dist).
### [x] 2. (CLAUDE) Server: GET /api/areas/:file/spawn aggregate + tests
- Do: read-only route returning simulateResets output for the parsed on-disk model with the world-index resolver wired (map.ts idiom: never-crash, 404/400 guards, unparseable → 4xx with parse error). Tests: stock-like fixture with an M/G/E chain + cross-area P, broken file tolerated, 404/400.
- Files: /workspace/shattered-archive/apps/mud-builder-server/src/routes/spawn.ts (new), src/routes/spawn.test.ts (new), src/app.ts
- Verify: host mud-builder-server suite green; tsc clean.
### [x] 3. (CLAUDE) Client: Simulate views + tests
- Do: Resets tab gains a "Simulate" pane (per-room accordion of spawned mobs w/ gear trees, objects w/ contents, door states, warnings up top); Areas preview pane links room → its spawn slice; boot-state disclaimer shown. Tests: mocked /spawn render (gear tree + warning), room filter.
- Files: /workspace/shattered-archive/apps/mud-builder-client/src/features/resets/ResetsPage.tsx, src/features/resets/SimulatePane.tsx (new, +test), src/api/client.ts
- Verify: host mud-builder-client suite green; tsc clean.
### [x] 4. (CLAUDE) Map overlay: spawn badges
- Do: MapPage area mode optionally overlays per-room mob counts (badge on the room node) from /spawn; toggle in the map toolbar; world mode untouched. Tests: badge renders from mocked spawn data, toggle hides.
- Files: /workspace/shattered-archive/apps/mud-builder-client/src/features/map/MapPage.tsx (+test), src/api/client.ts
- Verify: host mud-builder-client suite green; tsc clean.
### [x] 5. (CLAUDE) Deploy + live E2E + docs + sign-off
- Do: rebuild+up builder pair; E2E via edge: /spawn for midgaard matches known stock facts (e.g. the temple healer/guards spawn counts, a shopkeeper carries its wares), warnings empty on stock corpus or explained, game container untouched (StartedAt ordering). Update docs/mud-builder/README.md (+Scope) and .annotated; mark plan COMPLETE; draft Phase 14 (candidates: in-UI new-skill C codegen assist — needs the stability discussion; map room-drag exit editing; live repop-drift view via a game-side read-only socket).
- Files: (driver in scratchpad), /workspace/shattered-archive/docs/mud-builder/README.md
- Verify: all driver checks pass; suites green; the MUD never restarted this phase.

## Progress log

- 2026-07-17T16:15:00Z plan created (successor to Phase 12, which is COMPLETE); reset simulator chosen over the other candidates because it is pure-JS/read-only (stability-free), directly answers the most common builder question ("what does this area actually spawn?"), and compounds with the Phase 12 map (spawn badges); map exit-editing and the new-skill C codegen assist remain Phase 14 candidates (the latter still needs its stability discussion)
- 2026-07-17T18:00:00Z plan PARKED, not truly abandoned (qplan has no DEFERRED status): the user directed Phase 12b (mapping fidelity — expanded compass rose, teleports, one-ways, blocked paths + UX/auth corrections) to land FIRST. Re-set this doc to ACTIVE after 12b completes. Content stays valid; only step 4 (map spawn badges) may need rebasing on 12b's map render changes, and the simulator gains the 12b exit-limit facts (obj limits db.c:1380-1385) for free.
- 2026-07-18T01:40:00Z re-ACTIVATED: Phase 12b is COMPLETE and deployed. Notes for execution: doors now run 0-9 (D resets too) and #SCRIPTS has R room entries — the simulator's D handling must use the 10-door bounds; /api/world already computes limitPressure with the exact db.c limit decoding (>50→6, -1→999, routes/world.ts effectiveObjLimit) — reuse it; map spawn badges (step 4) land on the 12b MapPage which now draws classification markers + a legend, so badge placement should avoid the top-right self-loop ring corner.
- 2026-07-23T (Claude) step 1 DONE and boxed. Read merc-mud/2.4/src/db.c's actual reset_area (not from memory) to ground every branch: the shared `last`/`mob` ("LastMob") state threaded across the WHOLE reset list regardless of command type (so an unrelated D/O between an M and a G can keep or break the chain exactly like the real game); get_obj_type's global "most-recently-created object of this vnum" search (object_list is newest-first, since create_object prepends) for P's container lookup — modeled as a single `Map<vnum, node>` overwritten on every creation; P's chaining rule that a container just given to a mob (in_room===null) still accepts a P as long as `last` is still true; the exact M-limit (direct arg2 compare, no >50/-1 decoding — that decoding is P/G/E-only) vs P/G/E-limit (effectiveObjLimit, duplicated locally rather than importing the server's routes/world.ts copy — wrong dependency direction, merc-area is the dependency-free base package) distinction; that db.c only bug()s the G/E `mob===NULL` case, NOT the general `!last` chain-break, so only the former produces a simulator warning (caught this via a test that initially asserted the wrong thing, then re-checked against the source and fixed the test, not the code); doors start OPEN at load regardless of the room's own `locks` field (EX_CLOSED is only ever set by a #RESETS D command, never at parse time) so a door nobody resets shows as 'open', not 'closed'. Wrote `services/merc-area/src/simulate.ts` (`simulateResets`, `WEAR_SLOTS`, result/option types) + `simulate.test.ts` (15 cases covering M+E+G chain, global vs room-local M limits, O dedup, P into a room container, P chaining onto a just-given container, D lock states 0/1/2 + no-warning on a missing exit, R reported without reordering, both orphan-G/E cases — warned (no mob ever) vs silent (chain broken), cross-area resolver, unresolvable-vnum-no-resolver, identical-loadout grouping) — all passed on the FIRST real run except one self-inflicted test-data bug (arg2=0 on G/E/P test fixtures collided with my own deterministic "at-limit" simplification, since effectiveObjLimit(0)=0; fixed by using -1/"unlimited" in those fixtures, matching real area-file convention). `pnpm --filter @shatteredarchive/merc-area test`: 89/89 (74 pre-existing + 15 new). `pnpm --filter @shatteredarchive/merc-area build`: clean. Exported from `index.ts`; `.annotated`/`.ai-context` refreshed. Documented simplifications (not modeled, and why) live in `simulate.ts`'s own file-header comment: the P/G/E limit's 1-in-5 random bypass, shop-keeper unlimited restock, pet-shop/cost/alignment-zap side effects, container lock write-back. Next: step 2 (server route).
- 2026-07-23T (Claude) step 1 sign-off: re-verified from a clean state rather than trusting the entry above — reran `pnpm --filter @shatteredarchive/merc-area build` (clean) and `test` (89/89) fresh, then smoke-tested `simulateResets` against the REAL stock fixtures (`src/__fixtures__/midgaard.are`, `limbo.are`), not just synthetic unit data. midgaard.are (no resolver configured): 65 rooms with spawns, 86 mob instances, 21 room-level objects, 17 doors, 0 randomized rooms, 5 warnings — all "room/object N not found", each a genuine out-of-file cross-area reference (e.g. room 1200/1116/1144, outside midgaard's own vnum range) that step 2's world-index resolver will resolve away; zero warnings were spurious/wrong. Spot-checked room 3001's output: mob 3011 "Hassan" wielding "Hassan's scimitar" (vnum 3005, slot "wielded") — a real, recognizable stock NPC+weapon pairing, not a plausible-looking fabrication. limbo.are (a near-empty utility area with no #RESETS content): 0 spawns, 0 warnings, no crash. No code changes were needed — this pass only added confidence, it found nothing to fix. Step 1 confirmed COMPLETE.
- 2026-07-23T (Claude) step 2 DONE and boxed. Compared two existing single-file-route idioms before picking one: routes/map.ts's simpler catch (AreaStoreError→its status, else 500) vs routes/areas.ts's `safe()` (adds ParseError/EmitError→400). map.ts's own single-file route has never actually promised 400 on a parse failure (untested, would 500 today), but this step's own spec explicitly requires "unparseable → 4xx with parse error" — so spawn.ts mirrors areas.ts's `safe()` taxonomy instead (trimmed: no AreaConflictError/EmitError, this route never writes). Cross-area resolution reuses AreaStore.resolveRefs' exact pattern (`area-store.ts:212-217`) inline: `store.worldVnumIndex(file)` then `resolveExternal: (kind,vnum) => index[kind].get(vnum) ?? null`, passed straight to `simulateResets`. Wrote `apps/mud-builder-server/src/routes/spawn.ts` (`registerSpawnRoutes`) + `spawn.test.ts` (4 cases: an M/E/G-style chain + a cross-area P — a local chest holding an object whose TEMPLATE is only defined in a second `equipment.are` fixture file, proving the real worldVnumIndex resolver reaches simulateResets end-to-end and not just a stub; a broken neighbor file in area.lst doesn't break a healthy file's own spawn preview — AreaStore.worldVnumIndex already swallows per-neighbor parse failures, asserted explicitly so a regression here is caught; requesting spawn for the broken file itself 400s with the parse error, not 500/crash; 404 missing file, 400 invalid file name). Wired into `app.ts` next to `registerMapRoutes`. All 4 tests passed on the first real run. `pnpm --filter @shatteredarchive/mud-builder-server build`: clean. `pnpm --filter @shatteredarchive/mud-builder-server test`: 85/85 (81 pre-existing + 4 new). Deferred to step 5 by design (not an oversight): no live E2E against a running server this step — step 1 already live-smoke-tested the underlying simulateResets against real midgaard.are, and this step's own Verify line only asks for the suite + tsc, reserving live E2E for step 5's deploy pass. `.annotated` refreshed for `routes/spawn.ts`, `routes/spawn.test.ts`, and `app.ts`'s entry. Next: step 3 (client Simulate view).
- 2026-07-23T (Claude) step 3 DONE and boxed. Oriented in `ResetsPage.tsx`, `api/client.ts`, `WorldPage.tsx`/`MapPage.tsx` (fetch-on-mount + `useCallback`/`<details>` idioms) before writing anything. Added `api.spawn(file)` to `client.ts`, typed via `SimulateResetsResult` imported directly from `@shatteredarchive/merc-area` (no local type duplication needed, unlike the server-route-specific `AreaMapResponse` etc., since merc-area is shared isomorphic code both client and server already depend on). Wrote `SimulatePane.tsx`: merges the API's separate `rooms`/`doors`/`randomizedExits` lists into one per-room `RoomView` client-side (the API itself stays as tested in step 2 — this is presentation-only), a per-room `<details>` accordion (mobs w/ gear trees — equipped slot label + recursive carried/P contents, room objects, door states, a randomized-exits marker), warnings up top, a boot-state disclaimer, a room filter (vnum or resolved room name via a local `Room.name` lookup built from `wb.area`), and a Refresh button. Wired into `ResetsPage.tsx` unconditionally below the reset-editing form (same on-disk-snapshot convention as World/Map — it does NOT reflect unsaved edits, only the last save).
  Caught and fixed a real regression before it shipped: `ResetsPage.test.tsx`'s fetch mock throws on any unrecognized URL, and SimulatePane's new unconditional fetch to `.../spawn` would have hit that throw on every existing test — added a `/spawn` case to the mock rather than leaving tests coincidentally passing through an error path.
  Corrected a wrong assumption from this plan's own Context section: it named `PreviewPane.tsx` as where a "room → spawn slice" link belongs, but `PreviewPane.tsx` is a raw text-diff view with no per-room granularity to attach a link to (confirmed by reading it, not assuming) — `RoomEditor.tsx` (the actual single-room structured editor, rendered once per selected room) is the real fit. Added an optional `onOpenSpawn?: (vnum) => void` prop there rendering a "See what spawns here →" link, threaded `AreasPage.tsx` → `App.tsx`, which lifts it the same way the existing `areaTarget`/World-dashboard pattern already does (a fresh `{vnum}` object per click so the identity always changes, mirroring how `ExternalRef` literals already made that pattern work by accident) into a new `resetsRoomTarget` state, switching to the Resets tab and passing `initialRoomTarget` down into `SimulatePane`, which seeds the filter and auto-opens the single matching room's `<details>`.
  Tests: `SimulatePane.test.tsx` (7 cases: gear tree incl. nested P contents, warnings, disclaimer, room filter, cross-tab target seeding, fetch-error non-crash, Refresh) + 1 new case in `RoomEditor.test.tsx` (link absent without the prop, calls back with the vnum when clicked). `pnpm --filter @shatteredarchive/mud-builder-client test`: 100/100 (92 pre-existing + 8 new). `npx tsc --noEmit`: clean (the plain `vite build` alone does not typecheck — ran `tsc` separately since that's the actual gate).
  Live verification beyond the plan's own ask (its Verify line only requires the suite + tsc): no browser-automation tool is available in this environment (`chromium-cli`/Playwright/Puppeteer all absent) and installing one wasn't done unilaterally for a one-off check, so this could NOT be visually screenshot-verified in an actual browser — noted explicitly rather than claimed. Instead, started REAL `mud-builder-server` + `mud-builder-client` dev processes and hit the real network path a browser would take: `curl http://localhost:60080/api/areas/midgaard.are/spawn` (the CLIENT dev server's own port, proxied to the server per `vite.config.ts`) returned the identical result as hitting the server directly (68 rooms, 0 warnings — up from step 1's standalone smoke test's 65 rooms/5 warnings, now that the real `worldVnumIndex` resolver is wired through the route), proving the full client→proxy→server→simulator→resolver chain is live and correct end to end, not just unit-tested in isolation. Killed both processes via `netstat`+`taskkill` (confirmed dead via failed health-check curls), deleted scratch log/JSON files. No `.env` changes were needed this time (spawn is read-only; the default config already pointed at a valid `merc-mud` checkout with writes off).
  `.annotated` refreshed for `src/`, `src/api/`, `src/features/areas/`, `src/features/resets/`. Next: step 4 (map spawn badges).
- 2026-07-23T (Claude) step 4 DONE and boxed. MapPage (area mode only) gained a "Spawns"
  toolbar checkbox — off by default and fetch-on-demand: `api.spawn(file)` (already added in
  step 3, so the step's listed `src/api/client.ts` needed NO change this time) is called only
  while the toggle is on, per-room mob counts are summed from `rooms[].mobs[].count`
  client-side, and rooms with a non-zero count get a green count badge on the room node's
  top-LEFT corner — deliberately opposite the 12b self-loop ring's top-right corner, per this
  plan's own 2026-07-18 re-activation note. Badge lives INSIDE the room's `<g role="button">`
  (clicking it still opens the room in Areas; the room's explicit `aria-label` means the badge
  digit never leaks into the accessible name, so all pre-existing name-based queries held
  without edits) with its own `<title>` pointing at Resets ▸ Simulate. The legend grows a
  "mobs at boot (count)" swatch only while the overlay is on; a failed spawn fetch renders a
  visible "Spawn overlay unavailable: …" line instead of silently showing nothing (never-crash,
  never-mislead); world mode untouched. Stale-overlay guard: the fetch effect nulls the counts
  before each load so switching areas can't briefly paint the previous area's badges onto the
  new grid. Tests: 2 new MapPage cases (toggle-on renders the badge with the summed count 3
  from mocked /spawn data, asserts NO /spawn fetch ever fires while off, object-only room gets
  no badge, legend entry appears; toggle-off hides badge + legend again). `pnpm --filter
  @shatteredarchive/mud-builder-client test`: 102/102 (100 pre-existing + 2 new). `npx tsc
  --noEmit` in the client package: clean (same "vite build doesn't typecheck" gate as step 3).
  `.annotated` refreshed for `src/features/map/` (MapPage, its test, map.css) and MapPage's
  `@ai-summary`/`@ai-notes` header updated. Next: step 5 (deploy + live E2E + docs + sign-off).
- 2026-07-23T step 5 DONE; plan COMPLETE. Deploy: recorded merc-mud2.4's StartedAt
  (2026-07-23T17:57:47.601244647Z) BEFORE touching anything, then `docker compose -p
  shatteredarchive -f deploy/docker-compose.shattered-archive-experimental.yml build
  mud-builder-server mud-builder-client` + `up -d` those two services only — both recreated
  and healthy in ~26s; StartedAt re-checked mid-deploy AND after the E2E pass: byte-identical,
  the game container was never touched. Live E2E via the real edge
  (https://build.shatteredarchive.dev, node --use-system-ca for the locally-trusted CA):
  10/10 checks in a scratchpad driver whose stock facts were read from the LIVE
  midgaard.are's #RESETS lines first (grep'd, not recalled) — /spawn 200 with 68 rooms;
  room 3001 Hassan (3011) wielding scimitar 3005; room 3011 weaponsmith (3003) carrying all
  10 G-reset wares 3020-3029 AND wielding the E-reset long sword 3022 (the "shopkeeper
  carries its wares" bar, plus proof G and E create separate instances); room 3054 donation
  pit (O 3010) + healer (M 3012) count 1; warnings [] on the stock corpus (the plan's
  "empty or explained" bar — empty, nothing to explain); UI serves at the edge; /api/map
  midgaard 200 with 143 rooms. Driver deleted after the pass. Docs: README gained a
  "Reset simulator (Phase 13)" section (db.c-faithful semantics incl. the LastMob chain
  subtlety, gear trees, warnings-not-silence, boot-state disclaimer, both click-throughs,
  read-only/last-saved caveat) and the Scope paragraph a Phase 13 sentence;
  docs/mud-builder/.annotated refreshed. The MUD never restarted this phase — every layer
  (simulator, route, panes, badges) is read-only by construction. Phase 14 candidates for
  the user to choose from (per this step's Do line, recorded not decided): (a) in-UI
  new-skill C codegen assist — MUST have the stability discussion first; (b) map room-drag
  exit editing; (c) live repop-drift view via a game-side read-only socket. Status: COMPLETE.
