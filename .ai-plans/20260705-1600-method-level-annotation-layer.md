# Plan: Method-level annotation layer (AI-METHOD blocks)

Created: 2026-07-05T16:00:00 · Workspace: /workspace/shattered-ai · Status: COMPLETE
Task: Add a fourth annotation layer — per-method AI blocks — to the shattered_mcp server, with a passive loop, Continue prompts, and a Claude Code hook for host-side edits.

## Goal
Extend the existing annotation system (folder `.annotated`, file-top `AI-ANNOTATION`) with a per-method layer: an idempotent `AI-METHOD` comment block above each function/method giving a one-line purpose, regenerated safely on each run, staleness-aware via a body hash, and maintained passively. Any AI edit — Continue (already calls `markDirty`) or Claude host-side (via a new PostToolUse hook feeding a queue file) — schedules re-annotation. Done when: running the new `annotate_methods` tool on a `.ts`/`.cs`/`.kt`/`.swift` file inserts `AI-METHOD` blocks above methods without touching human comments, a second run is a no-op on unchanged methods, and editing a method body causes only that method's block to refresh.

## Constraints
- All annotation inference uses the local qwen model via `withOllamaSlot` (free, GPU-serialized) — NEVER the Anthropic API. Model default `qwen3.6`, `keep_alive: "1m"`.
- NON-DESTRUCTIVE: only the marker-delimited `AI-METHOD` range is ever inserted/replaced. Human comments, JSDoc, and XML-doc (`///`) lines are never modified.
- Heuristic regex detection only (no new parser/AST deps). Languages this cut: `.ts .tsx .js .jsx .mjs .cjs .cs .kt .kts .swift` — all brace-delimited. Unmatched/ambiguous constructs are SKIPPED, never guessed.
- Reuse existing infra: `commentStyleFor`, `annotationField`, `withOllamaSlot`, `annotateIsSkippable`, the dirty-dir/auto-refresh machinery, and the `.mcp-settings.json` toggle pattern. Do not fork them.
- Keep passive work deliberately slow (bounded files per tick) so it never competes hard for the GPU.

## Context
- Server (single file, ~4300 lines): /workspace/shattered-ai/mcp-ollama/src/index.ts
  - `annotate_block` idempotent upsert (the pattern to mirror per-method): ~line 4198; `buildAnnotationBlock`/`commentStyleFor`/`annotationField`/`COMMENT_STYLE_BY_EXT` (already includes `.swift`): ~4144-4196. Markers `ANNOTATION_BEGIN_MARK`/`END`: ~4141.
  - `annotateSingleFile` (qwen one-liner prompt pattern): ~3585. `annotateFolder`: ~3656. `annotateIsSkippable`: ~3577.
  - `markDirty` (Continue-side edits queue a folder): ~1376; wired at write/edit/delete tool sites ~2565/2619/2868/2954. `runAutoRefresh`: ~1417.
  - Staleness scanner (mtime vs `.annotated`): ~1489. Passive discovery loop `passiveAnnotateTick` + `findUnindexedDirs` + settings read/write (`readMcpSettings`/`writeMcpSettings`, `MCP_SETTINGS_PATH`): ~1551-1711. `passive_annotate` control tool: ~1713.
- Continue prompts (model contract = ONE tool call each, `/no_think`): /workspace/shattered-ai/.continue/prompts/annotate-block.prompt, passive-annotate.prompt, annotate-tree.prompt — mirror these.
- Build/deploy: /workspace/shattered-ai/mcp-ollama — `pnpm build` (`tsc` → `build/index.js`); container `shattered_mcp` via docker-compose.yml (+ docker-compose.cuda.yml). Verify with `docker logs shattered_mcp`.
- Claude Code hooks (host side, NOT container — these steps are CLAUDE-only): C:/Projects/ShatteredArchive/.claude/settings.json (existing PreToolUse Read/Bash guards + UserPromptSubmit) and C:/Projects/ShatteredArchive/.claude/hooks/*.js. Host→container path map: `C:/Projects/Shattered-AI`→`/workspace/shattered-ai`, `C:/Projects/ShatteredArchive`→`/workspace/shattered-archive`, etc.
- AI-METHOD block format (exact — keep consistent across executors), using the file's comment style:
  ```
  ── AI-METHOD ── regenerated by annotate_methods — do not hand-edit
  @ai-method: <name> — <≤15-word purpose>
  @ai-hash: <first 8 hex of sha256 of the normalized method body>
  ── END AI-METHOD ──
  ```
  Placed on the line(s) immediately ABOVE the method signature, below any human doc comment. "Normalized body" = signature+body text with any nested AI-METHOD/AI-ANNOTATION lines stripped and whitespace collapsed. A method is *annotated & fresh* iff an AI-METHOD block sits directly above it whose `@ai-hash` equals the current normalized-body hash; otherwise it is (re)generated.

## Steps
### [x] 1. (CLAUDE) Method-extraction + AI-METHOD block engine
- Do: In index.ts add pure helpers near the annotate_block section: `extractMethods(filePath, content)` → `{name, sigLineIdx, bodyStart, bodyEnd, existingBlockRange?}[]` using per-language regex for brace-delimited signatures (TS/JS: `function`, arrow assigned to const, class methods; C#: access-modifier + return-type + name + params + `{`, skip lines under `///`; Kotlin: `fun`; Swift: `func`), then brace-matching to find body end (respecting strings/line-comments minimally). Add `normalizeMethodBody`, `hashBody` (sha256 first 8 hex, use node `crypto`), `buildMethodBlock(style, name, purpose, hash)` (mirrors `buildAnnotationBlock`), and `upsertMethodBlock(lines, method, block)` that replaces an existing marker range directly above the signature or inserts there. No tool registration yet.
- Files: /workspace/shattered-ai/mcp-ollama/src/index.ts
- Verify: `cd /workspace/shattered-ai/mcp-ollama && pnpm build` compiles clean; a scratch `tsx` snippet running `extractMethods` on index.ts itself returns a plausible method list (names match real functions, no overlaps).

### [x] 2. (CLAUDE) `annotate_methods` MCP tool (single file)
- Do: Register `annotate_methods` ({file_path, model?}) mirroring `annotate_block`. Read the file, `extractMethods`, and for each method that is absent-or-stale (hash mismatch) call qwen via `withOllamaSlot` with a one-line-purpose prompt (adapt `annotateSingleFile`'s prompt: feed signature + body, ask ≤15-word purpose), then `upsertMethodBlock`. Skip fresh methods with no GPU call. Write the file once, report `<n> annotated, <m> fresh (skipped), <k> methods total`. Guard: file-type in the supported set, size ≤1 MB, else return a clear message.
- Files: /workspace/shattered-ai/mcp-ollama/src/index.ts
- Verify: after `pnpm build` + container restart, call the tool on one server/app file; confirm AI-METHOD blocks appear above methods, human comments untouched, a second call reports all-fresh/0-annotated, and hand-editing one method body then re-running refreshes ONLY that block.

### [x] 3. (qwen) Passive method loop + dirty-queue drain + control tool
- Do: Add `passiveAnnotateMethodsTick` (gated by `readMcpSettings().passiveAnnotateMethods === true`, own `setTimeout`/`setInterval` like `passiveAnnotateTick`, one file per tick from a bounded walk of folders that HAVE a `.annotated`, choosing files with absent/stale method blocks). Also drain a queue file `METHOD_QUEUE_PATH` (default `/workspace/shattered-ai/.method-annotate.queue`, one `/workspace/...` path per line — fed by the Claude hook and optionally `markDirty`): pop paths, run the step-2 logic, truncate. Register `passive_annotate_methods` control tool (on/off/status → `{enabled}`/`{}`) mirroring `passive_annotate`, persisting via `writeMcpSettings`.
- Files: /workspace/shattered-ai/mcp-ollama/src/index.ts
- Verify: `pnpm build` clean; with the flag on, appending a file path to `.method-annotate.queue` results in that file gaining/refreshing AI-METHOD blocks within one tick (check `docker logs shattered_mcp` for the annotate line); `passive_annotate_methods` with no args reports status.

### [x] 4. (qwen) Continue prompts for the new tools
- Do: Add `/workspace/shattered-ai/.continue/prompts/annotate-methods.prompt` (one-shot: resolve Windows→`/workspace` path, then ONE `shattered_archive_annotate_methods` call, print reply — mirror annotate-block.prompt) and `passive-annotate-methods.prompt` (toggle: map on/off/status → args, ONE `shattered_archive_passive_annotate_methods` call — mirror passive-annotate.prompt). Refresh the folder's `.annotated`/`.ai-context` for the two new files.
- Files: /workspace/shattered-ai/.continue/prompts/annotate-methods.prompt, /workspace/shattered-ai/.continue/prompts/passive-annotate-methods.prompt, /workspace/shattered-ai/.continue/prompts/.annotated
- Verify: both prompt files have valid `--- name/description ---` frontmatter and reference the exact tool names registered in steps 2-3; `.annotated` lists them.

### [x] 5. (CLAUDE) Claude Code PostToolUse hook feeding the queue
- Do: Add C:/Projects/ShatteredArchive/.claude/hooks/method-annotate-queue.js: on Edit|Write|MultiEdit, read the tool payload, take the edited file path, map host `C:/Projects/<Repo>` → `/workspace/<kebab>` (same kebab rule as annotate-tree.prompt), skip unsupported extensions/skippable names, and append the container path to the queue file on the mounted workspace (dedupe against last line, best-effort, never block the edit). Wire a `PostToolUse` matcher `Edit|Write|MultiEdit` into .claude/settings.json.
- Files: C:/Projects/ShatteredArchive/.claude/hooks/method-annotate-queue.js, C:/Projects/ShatteredArchive/.claude/settings.json
- Verify: editing a `.ts` file under a workspace root appends its `/workspace/...` path to `.method-annotate.queue`; editing a `.md`/skippable file does not; hook exits 0 and never blocks the edit even if the queue path is unwritable.

### [x] 6. (CLAUDE) Build, deploy, end-to-end verify, refresh indexes
- Do: `pnpm build` in mcp-ollama, rebuild/restart `shattered_mcp`. Exercise the full loop: Claude-edit a method → queue entry appears → passive tick (flag on) annotates it. Confirm idempotency + per-method staleness end to end. Refresh `@ai-` header / `.annotated` / `.ai-context` for touched folders (host-side edits don't auto-refresh). Note the new tools + hook in the architecture/qdigest-adjacent docs if warranted.
- Files: /workspace/shattered-ai/mcp-ollama/src/index.ts, /workspace/shattered-ai/mcp-ollama/.annotated, /workspace/shattered-ai/doc/ai-context-conventions.md
- Verify: `docker logs shattered_mcp` shows the tools registered and a passive method-annotation line; a fresh file gains blocks, an unchanged re-run is a no-op, an edited method refreshes only its block; indexes/doc updated.

## Progress log

- 2026-07-05T16:00:00 plan created
- 2026-07-05T16:40:00 step 1 done: engine (tokenizer/depths/brace-match/signature regex/extractMethods + normalize/hash/buildMethodBlock) added to index.ts before annotate_block; crypto imported. Verified: `pnpm build` clean; scratch check on index.ts finds 92 real methods (0 overlaps, 0 bad spans) and multi-lang fixtures pass (Kotlin `= expr` body, Swift computed prop, C# string-brace, object-return arrows, and setTimeout/resolve/async-callback false positives all correctly skipped via the paren-0 body-brace rule + "async" name block).
- 2026-07-05T17:05:00 step 2 done: `annotate_methods` tool + shared `annotateFileMethods` core (bottom-up splice apply, per-method absent/stale check, `summarizeMethod` qwen call) registered after annotate_block. `pnpm build` clean.
- 2026-07-05T17:20:00 step 3 done: `passiveAnnotateMethodsTick` (queue-drain then discovery, gated by `passiveAnnotateMethods` flag), `.method-annotate.queue` read/write + `enqueueMethodPath`, `findMethodTarget`/`fileNeedsMethodWork`, `passive_annotate_methods` control tool; `markDirty` now enqueues Continue-side content edits. `pnpm build` clean.
- 2026-07-05T17:30:00 step 4 done: `annotate-methods.prompt` + `passive-annotate-methods.prompt` added (mirror annotate-block/passive-annotate, one tool call each); `.continue/prompts/.annotated` refreshed.
- 2026-07-05T17:40:00 step 5 done: `.claude/hooks/method-annotate-queue.js` (PostToolUse Edit|Write|MultiEdit → host→/workspace kebab map → append to queue) + settings.json wiring. Verified via harness: backslash/forward paths map correctly, dsl-log-viewer kebab, .md/node_modules/.d.ts skipped, dedup works, always exit 0.
- 2026-07-05T17:55:00 step 6 done: mechanical GPU-free test proves insert-above-cluster (human comment + JSDoc preserved), idempotent re-run (0 annotated/2 fresh, byte-identical, no model call), and per-method staleness (edit one body → only that block refreshes). Rebuilt image + recreated `shattered_mcp` via docker-compose.cuda.yml; server up on :3100, no errors; deployed build/index.js contains annotate_methods/passive_annotate_methods/AI-METHOD/annotateFileMethods. Docs: AI-METHOD section added to ai-context-conventions.md; src/.annotated index.ts one-liner refreshed. Actual qwen summary quality left to the user (per "no silent GPU verification").
- 2026-07-05T17:56:00 plan COMPLETE — all 6 steps verified.
