{
  "name": "shattered-archive-web-server",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start:dev": "npx nodemon --exec ts-node -r tsconfig-paths/register app.ts --port 8081 --execEnv dev",
    "start:prod": "npx nodemon --exec ts-node -r tsconfig-paths/register app.ts --port 8081 --execEnv prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
