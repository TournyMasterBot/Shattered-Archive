The level folder contains functionality for processing level-up notifications from the probe, specifically parsing lines to extract character stat gains. It fits into the project as part of the data processing pipeline that handles game event notifications and extracts meaningful information from raw log data. The probe-level-up.ts file serves as the core parser for interpreting level-up events and their associated stat changes.
