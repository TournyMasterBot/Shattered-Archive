The level folder contains functionality for processing level-up notifications from the probe, specifically parsing character stat gains from log lines. It fits into the project as part of the data processing pipeline that extracts and transforms game event information. The probe-level-up.ts file serves as the core parser for handling level-up events and their associated stat modifications.
