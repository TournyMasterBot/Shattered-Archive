The userStyles folder manages user-defined CSS overrides and high-contrast settings that persist across sessions using localStorage. It allows users to customize the application's appearance with custom styles and accessibility preferences. This folder integrates with the broader application by providing a centralized store for user preferences that can be applied dynamically to the UI.
