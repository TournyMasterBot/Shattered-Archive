The commands folder contains the core logic for handling outbound messaging operations, with outbound-queue.ts specifically managing message queuing and throttled sending through microtask-based pumping. This folder serves as the primary execution layer for sending messages, coordinating with other components like the message builder and transport mechanisms. It fits into the project as the central hub for outbound communication processing, ensuring messages are properly queued and sent according to configured throttling rules.
