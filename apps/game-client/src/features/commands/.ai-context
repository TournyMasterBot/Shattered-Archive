The commands folder contains modules responsible for managing outbound communication with the client, specifically handling command queuing and throttled sending to prevent overwhelming the client. The outbound-queue.ts file implements the core logic for managing this queuing system. This folder serves as the primary interface for sending commands to the client while maintaining proper rate limiting and throughput control.
