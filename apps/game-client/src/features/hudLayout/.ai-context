The hudLayout folder manages HUD (heads-up display) settings that persist across sessions using localStorage. It provides stores for layout mode preferences (classic vs. compact) and theme selection (default vs. slate-amber), enabling users to customize their HUD appearance. This folder integrates with the broader application by providing a centralized store for HUD preferences consumed by the MainContainer and settings UI.
