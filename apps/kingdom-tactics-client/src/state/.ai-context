The state folder holds the client's small, dependency-free app state: a shared EngineProviders singleton (providers.ts) that is the sole gateway to game rules/data/modes, and a screen-navigation store (nav.tsx) built from React context + useReducer that selects which full-screen surface renders and carries a match-setup payload into the match screen. Deliberately no router or external state library in v1.
