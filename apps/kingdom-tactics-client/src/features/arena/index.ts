// Arena feature slice — the grid battlefield renderer + the engine-driven match hook.
export { Arena, type ArenaProps } from './Arena';
export { useMatch, type UseMatch, type UseMatchOptions } from './hooks/useMatch';
