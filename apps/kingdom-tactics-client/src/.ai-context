The src folder contains all the source code for the Kingdom Tactics client application, serving as the core development directory where the main entry point (main.tsx) renders the React application and the primary landing page (App.tsx) provides project status overview. This folder houses the essential files that define the application's structure, styling (App.css), and TypeScript environment (env.d.ts) while maintaining a clean separation between source files and build outputs.
