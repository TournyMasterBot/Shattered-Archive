The src folder contains all the source code files for the game server application, with index.ts serving as the main entry point that initializes the environment, HTTP server, and WebSocket services. This folder houses the core logic and functionality that makes the game server operational. The src directory structure keeps all application code organized and separate from configuration files, dependencies, and other project assets.
